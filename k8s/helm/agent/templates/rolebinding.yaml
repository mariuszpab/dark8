apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "dark8-agent.fullname" . }}-rolebinding
subjects:
  - kind: ServiceAccount
    name: {{ include "dark8-agent.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
roleRef:
  kind: Role
  name: {{ include "dark8-agent.fullname" . }}-role
  apiGroup: rbac.authorization.k8s.io
